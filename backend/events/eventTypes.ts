export enum EventTypes {
  Created = "created",
  Updated = "updated",
  Deleted = "deleted",
  Moved = "moved",
  CreateBranch = "createBranch",
}

export enum EventNamespaces {
  GitLab = "gitlab",
  Task = "task",
}
